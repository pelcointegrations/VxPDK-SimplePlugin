<UserControl x:Class="OverlayDrawings.View.NewLineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:local="clr-namespace:OverlayDrawings"
             mc:Ignorable="d" Height="Auto" Width="Auto"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <GroupBox x:Name="StartPointGroup" FontWeight="Medium" Header="Start Point" Margin="5,20,5,5" Width="Auto" Height="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Label ToolTip="Line start x-coordinate as a percentage [0.0, 1.0]" x:Name="StartXLabel" Content="X:" FontWeight="Normal" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                <TextBox x:Name="StartXText" Grid.Column="1" Margin="5,5,5,5" Text="{Binding Path=X1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <Label ToolTip="Line start y-coordinate as a percentage [0.0, 1.0]" x:Name="StartYLabel" Content="Y:" FontWeight="Normal" HorizontalAlignment="Right" Grid.Row="1" VerticalAlignment="Center"/>
                <TextBox x:Name="StartYText" Grid.Row="1" Grid.Column="1" Margin="5,5,5,5" Text="{Binding Path=Y1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </GroupBox>
        <GroupBox x:Name="EndPointGroup" FontWeight="Medium" Header="End Point" Margin="5,5,5,5" Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Label ToolTip="Line end x-coordinate as a percentage [0.0, 1.0]" x:Name="EndXLabel" Content="X:" FontWeight="Normal" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                <TextBox x:Name="EndXText" Grid.Column="1" Margin="5,5,5,5" Text="{Binding Path=X2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <Label ToolTip="Line end y-coordinate as a percentage [0.0, 1.0]" x:Name="EndLabel" Content="Y:" FontWeight="Normal" HorizontalAlignment="Right" Grid.Row="1" VerticalAlignment="Center"/>
                <TextBox x:Name="EndYText" Grid.Row="1" Grid.Column="1" Margin="5,5,5,5" Text="{Binding Path=Y2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </GroupBox>
        <Grid Grid.Row="2 " Margin="5,10,0,5" HorizontalAlignment="Right">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="260"/>
            </Grid.ColumnDefinitions>
            <Label ToolTip="Color of the line" x:Name="LineColorPicker" FontWeight="Normal" Content="Line Color:" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            <xctk:ColorPicker Grid.Column="1" Margin="5,5,5,5" SelectedColor="{Binding Path=LineColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <CheckBox Grid.Row="1" Grid.Column="2" Content="Animate" HorizontalAlignment="Center" VerticalAlignment="Center" IsChecked="{Binding Path=Animate, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Row="1" Grid.ColumnSpan="2" Content="Draw" BorderBrush="DarkSlateBlue" Padding="3" Margin="5" HorizontalAlignment="Right" Command="{Binding Path=DrawCommand}"/>
        </Grid>
    </Grid>
</UserControl>
