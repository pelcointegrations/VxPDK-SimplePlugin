<UserControl x:Class="OverlayDrawings.View.AllOverlaysView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OverlayDrawings.View"
             mc:Ignorable="d" 
             d:DesignHeight="Auto" d:DesignWidth="Auto"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="OverlaysGrid"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding Overlays}"
                  Width="400"
                  FontWeight="Medium"
                  Foreground="DarkSlateBlue"
                  Height="400"
                  Margin="5,20,5,5"
                  SelectionMode="Extended"
                  ScrollViewer.CanContentScroll="True"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>
                <DataGridTextColumn Header="Animated" Binding="{Binding  Animated}"/>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="1" HorizontalAlignment="Right">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button HorizontalAlignment="Right" Padding="3" FontSize="12" FontWeight="Medium" Margin="1,10,1,5" Content="Stop Animation" BorderBrush="DarkSlateBlue" Command="{Binding Path=StopAnimationCommand}" CommandParameter="{Binding ElementName=OverlaysGrid, Path=SelectedItems}"/>
            <Button Grid.Column="1" HorizontalAlignment="Right" Padding="3" FontSize="12" FontWeight="Medium" Margin="1,10,1,5" Content="Animate" BorderBrush="DarkSlateBlue" Command="{Binding Path=AnimateCommand}" CommandParameter="{Binding ElementName=OverlaysGrid, Path=SelectedItems}"/>
            <Button Grid.Column="2" HorizontalAlignment="Right" Padding="3" FontSize="12" FontWeight="Medium" Margin="1,10,1,5" Content="Remove" BorderBrush="DarkSlateBlue" Command="{Binding Path=RemoveCommand}" CommandParameter="{Binding ElementName=OverlaysGrid, Path=SelectedItems}"/>
            <Button Grid.Column="3" HorizontalAlignment="Right" Padding="3" FontSize="12" FontWeight="Medium" Margin="1,10,1,5" Content="Remove All" BorderBrush="DarkSlateBlue" Command="{Binding Path=RemoveAllCommand}"/>
        </Grid>
    </Grid>
</UserControl>
