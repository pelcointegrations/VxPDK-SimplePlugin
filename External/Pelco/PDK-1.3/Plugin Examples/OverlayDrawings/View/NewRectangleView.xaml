<UserControl x:Class="OverlayDrawings.View.NewRectangleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:local="clr-namespace:OverlayDrawings"
             mc:Ignorable="d" Height="Auto" Width="Auto"
             Background="White">
    <StackPanel>
        <GroupBox x:Name="UpperLeftPointGroup" FontWeight="Medium" Header="Upper Left Corner" Margin="5,20,5,5" Width="Auto" Height="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Label ToolTip="The % from the left side of the drawing area to place the upper left corner" Content="Left:" FontWeight="Normal" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" Margin="5,5,5,5" Text="{Binding Path=Left, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <Label ToolTip="The % from the top of the drawing area to place the upper left corner" Content="Top:" FontWeight="Normal" HorizontalAlignment="Right" Grid.Row="1" VerticalAlignment="Center"/>
                <TextBox Grid.Row="1" Grid.Column="1" Margin="5,5,5,5" Text="{Binding Path=Top, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </GroupBox>
        <GroupBox FontWeight="Medium" Header="Bottom Right Corner" Margin="5,5,5,5" Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Label ToolTip="The % from the left side of the drawing area to place the bottom right corner" Content="Right:" FontWeight="Normal" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" Margin="5,5,5,5" Text="{Binding Path=Right, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <Label ToolTip="The % from the top of the drawing area to place the bottom right corner" Content="Bottom:" FontWeight="Normal" HorizontalAlignment="Right" Grid.Row="1" VerticalAlignment="Center"/>
                <TextBox Grid.Row="1" Grid.Column="1" Margin="5,5,5,5" Text="{Binding Path=Bottom, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </GroupBox>
        <Grid Grid.Row="2" Margin="5,10,0,5" HorizontalAlignment="Right">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="260"/>
            </Grid.ColumnDefinitions>
            <Label ToolTip="Color of the line" FontWeight="Normal" Content="Border Color:" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            <xctk:ColorPicker Grid.Column="1" Margin="5,5,5,5" SelectedColor="{Binding Path=BorderColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Label ToolTip="Color to fill the rectangle" Grid.Row="1" FontWeight="Normal" Content="Fill Color:" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            <xctk:ColorPicker Grid.Row="1" Grid.Column="1" Margin="5,5,5,5" SelectedColor="{Binding Path=FillColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <CheckBox Grid.Row="2" Grid.Column="2" Content="Animate" HorizontalAlignment="Center" VerticalAlignment="Center" IsChecked="{Binding Path=Animate, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Row="2" Grid.ColumnSpan="2" Content="Draw" BorderBrush="DarkSlateBlue" Padding="3" Margin="5" HorizontalAlignment="Right" Command="{Binding Path=DrawCommand}"/>
        </Grid>
    </StackPanel>
</UserControl>
